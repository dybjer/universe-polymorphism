\begin{code}

-- {-# OPTIONS --without-K #-}

module MLTT.Test where

open import MLTT.UniverseStructure
open import MLTT.SuperUniverse

example : (𝓛 : Succ-Sup-Semilattice) → cumulative-on-the-nose 𝓛
example 𝓛 = record
              { U = {!!}
              ; T = {!!}
              ; ⌜ℕ₀⌝ = {!!}
              ; ⌜ℕ₁⌝ = {!!}
              ; ⌜ℕ⌝ = {!!}
              ; ⌜+⌝ = {!!}
              ; ⌜Π⌝ = {!!}
              ; ⌜Σ⌝ = {!!}
              ; ⌜W⌝ = {!!}
              ; ⌜Id⌝ = {!!}
              ; ⌜U⌝ = {!!}
              ; ⌜Lift⌝ = {!!}
              ; T-⌜ℕ₀⌝ = {!!}
              ; T-⌜ℕ₁⌝ = {!!}
              ; T-⌜ℕ⌝ = {!!}
              ; T-⌜+⌝ = {!!}
              ; T-⌜Π⌝ = {!!}
              ; T-⌜Σ⌝ = {!!}
              ; T-⌜W⌝ = {!!}
              ; T-⌜Id⌝ = {!!}
              ; T-⌜U⌝ = {!!}
              ; T-⌜Lift⌝ = {!!}
              }

\end{code}
